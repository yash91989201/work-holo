# Schema Convention

- [1. Schema Definition](#1-schema-definition)
- [2. Types](#2-types)
- [3. File Organization](#3-file-organization)
- [4. Consistency Rules](#4-consistency-rules)
- [5. Monorepo Structure](#5-monorepo-structure)
- [Example Project Structure](#example-project-structure)

## 1. Schema Definition

Always use **Zod** for schema definitions.

```ts
import { z } from "zod";

export const UserSchema = z.object({
  name: z.string(),
  age: z.number().min(0),
});
```

## 2. Types

- All types live in `src/lib/types.ts` — **do not edit this file manually**.
- Import types from `@/lib/types`, never directly from schema files.

```ts
import type { User } from "@/lib/types";
```

## 3. File Organization

- Schemas → `src/lib/schemas/` — one schema per file, no nested directories.
- Types → `src/lib/types.ts` (single file, auto-generated).

## 4. Consistency Rules

- Schema filenames: lowercase with dashes or underscores (e.g., `user-form.ts`).
- Always import types from `@/lib/types`, never from schema files directly.

## 5. Monorepo Structure

This is the **web** app in a monorepo (apps: `docs`, `native`, `server`, `web`).

The **server** maintains its own parallel structure for database tables and oRPC procedures:

| Location | Contains |
|---|---|
| `server/src/lib/schemas/` | DB + oRPC schemas |
| `server/src/lib/types/` | DB + oRPC types |

Path alias: `"@server/lib/*": ["../server/src/lib/*"]`

**Import rules:**
- Frontend schemas/types → `@/lib/schemas`, `@/lib/types`
- Backend schemas/types → `@server/lib/schemas`, `@server/lib/types`
- Use server types in frontend → import via `@server/lib/*`

## Example Project Structure

```
src/
  lib/
    schemas/
      user.ts
      user-form.ts
    types.ts          ← auto-generated, do not edit

server/
  src/
    lib/
      schemas/
      types/
```
